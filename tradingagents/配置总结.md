# TradingAgents 配置完成总结

## ✅ 已完成配置

恭喜！你的 TradingAgents 已经配置完成，现在有 **3 个 LLM API** 可用：

### 1. ✅ DeepSeek API（主要推荐）
- **API Key**: sk-d28ae30a58cb496c9b40e0029d0ef2c1
- **价格**: ¥1/百万tokens（性价比最高）
- **模型**: deepseek-chat, deepseek-reasoner
- **优势**: 中国可直接访问，稳定，快速
- **配置文件**: `run_with_deepseek.py`

### 2. ✅ Gemini API（免费额度）
- **API Key**: AIzaSyAIOWEahqzuBZV5Ne7sm_IylWrzSld-Euk
- **价格**: 免费（每天 1,500 次请求）
- **模型**: gemini-2.5-flash, gemini-2.5-pro
- **优势**: 推理能力强，免费额度多
- **限制**: 需要能访问 Google
- **配置文件**: `run_with_gemini.py`

### 3. ✅ 其他可用 API
- 智谱 GLM（国产，完全免费）
- 通义千问（阿里云，100万tokens/月）
- Groq（国际，超快速推理）

## 🚀 快速开始

### 方式 1：使用 DeepSeek（推荐）

```bash
cd TradingAgents
python run_with_deepseek.py
```

### 方式 2：使用 Gemini（免费）

```bash
cd TradingAgents
python run_with_gemini.py
```

### 方式 3：使用 CLI

```bash
cd TradingAgents
tradingagents
```

然后选择你想要的 LLM Provider。

## 📊 对比两个主要 API

| 特性 | DeepSeek | Gemini |
|------|----------|---------|
| **价格** | ¥1/百万tokens | 免费（1500次/天） |
| **速度** | 快 | 非常快 |
| **推理能力** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **中文支持** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **网络要求** | 直接访问 | 需要翻墙 |
| **稳定性** | 非常稳定 | 稳定 |
| **推荐场景** | 生产环境 | 测试、学习 |

## 📁 配置文件说明

### `.env` 文件
```
OPENAI_API_KEY=sk-d28ae30a58cb496c9b40e0029d0ef2c1  # DeepSeek
GOOGLE_API_KEY=AIzaSyAIOWEahqzuBZV5Ne7sm_IylWrzSld-Euk  # Gemini
```

### 运行脚本
- `run_with_deepseek.py` - 使用 DeepSeek 运行
- `run_with_gemini.py` - 使用 Gemini 运行
- `test_deepseek_simple.py` - 测试 DeepSeek
- `test_gemini_simple.py` - 测试 Gemini

## 🎯 使用建议

### 日常使用：DeepSeek
```python
config["llm_provider"] = "deepseek"
config["backend_url"] = "https://api.deepseek.com/v1"
config["deep_think_llm"] = "deepseek-chat"
```

**原因**：
- 性价比最高
- 网络稳定
- 中国可直接访问
- 中文友好

### 测试学习：Gemini
```python
config["llm_provider"] = "google"
config["deep_think_llm"] = "gemini-2.5-flash"
```

**原因**：
- 免费额度
- 推理能力强
- 适合实验

### 组合使用
```python
# 快速任务用 Gemini（免费）
config["quick_think_llm"] = "gemini-2.5-flash-lite"

# 深度推理用 DeepSeek（便宜）
config["deep_think_llm"] = "deepseek-chat"
```

## 📝 示例代码

### 基础使用
```python
from tradingagents.graph.trading_graph import TradingAgentsGraph
from tradingagents.default_config import DEFAULT_CONFIG
import os

# 使用 DeepSeek
config = DEFAULT_CONFIG.copy()
config["llm_provider"] = "deepseek"
config["backend_url"] = "https://api.deepseek.com/v1"
config["deep_think_llm"] = "deepseek-chat"
config["output_language"] = "Chinese"

ta = TradingAgentsGraph(debug=True, config=config)
_, decision = ta.propagate("BABA", "2025-01-15")
print(decision)
```

### 使用 Gemini
```python
# 使用 Gemini
config = DEFAULT_CONFIG.copy()
config["llm_provider"] = "google"
config["deep_think_llm"] = "gemini-2.5-flash"
config["output_language"] = "Chinese"

ta = TradingAgentsGraph(debug=True, config=config)
_, decision = ta.propagate("AAPL", "2025-01-15")
print(decision)
```

## 🔄 切换 API

### 从 DeepSeek 切换到 Gemini
1. 注释 .env 中的 `OPENAI_API_KEY`
2. 确保 `GOOGLE_API_KEY` 已设置
3. 运行 `run_with_gemini.py`

### 从 Gemini 切换到 DeepSeek
1. 注释 .env 中的 `GOOGLE_API_KEY`
2. 确保 `OPENAI_API_KEY` 已设置
3. 运行 `run_with_deepseek.py`

## ⚠️ 注意事项

### DeepSeek
- ✅ 网络稳定，中国可直接访问
- ✅ 性价比最高
- ✅ 中文支持好
- 💰 需要付费（但很便宜）

### Gemini
- ✅ 免费额度多
- ✅ 推理能力强
- ⚠️ 需要能访问 Google
- ⚠️ 可能有网络限制

## 🎓 下一步学习

1. **运行第一次分析**
   ```bash
   python run_with_deepseek.py
   ```

2. **尝试不同股票**
   - 美股: AAPL, TSLA, NVDA, BABA
   - 港股: 0700.HK, 9988.HK
   - A股: 600519.SS, 000001.SZ

3. **调整配置参数**
   - 辩论轮数
   - 输出语言
   - 模型选择

4. **集成 akshare**（待完成）
   - 更好的中国股市数据

## 📞 帮助文档

- TradingAgents GitHub: https://github.com/TauricResearch/TradingAgents
- DeepSeek 文档: https://platform.deepseek.com/docs
- Gemini 文档: https://ai.google.dev/docs
- 使用指南.md - 本地文档

## 🎉 恭喜

你已经成功配置了 TradingAgents！现在可以开始使用 AI 进行股票分析了。

**建议**: 先使用 DeepSeek 进行第一次分析，因为它最稳定可靠。

```bash
cd TradingAgents
python run_with_deepseek.py
```
